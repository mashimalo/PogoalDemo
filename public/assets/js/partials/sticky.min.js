$(window).scroll(function(){

    var scroll = $(window).scrollTop();

    if (scroll >= $('.has-sticky__bar').offset().top - 60) {

        $(".has-sticky__bar").addClass('sticky')

    } else {

        $(".has-sticky__bar").removeClass('sticky');

    }

});
//# sourceMappingURL=sticky.min.js.map
